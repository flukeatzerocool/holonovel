{
  "name": "holonovel-spec",
  "version": "2026.08.06",
  "description": "Build specification for TTRPG MCP game servers with convergence-loop quality enforcement and job-gated pre-build configuration.",
  "main": "index.js",
  "scripts": {
    "lint": "markdownlint holonovel.md README.md",
    "validate": "npx tsx scripts/validate.ts",
    "validate:traceability": "npx tsx scripts/validate.ts --traceability",
    "audit-assumptions": "npx tsx scripts/audit-assumptions.ts",
    "scan-ambiguity": "npx tsx scripts/scan-ambiguity.ts",
    "fmea": "npx tsx scripts/fmea.ts",
    "graph-deps": "npx tsx scripts/graph-deps.ts",
    "spec-health-trends": "npx tsx scripts/spec-health-trends.ts",
    "typecheck": "npx tsc --noEmit",
    "version-sync": "npx tsx scripts/version-check.ts",
    "clean": "find . -name '*.bak' -delete",
    "version-bump": "npx tsx scripts/version-bump.ts",
    "spec-delta": "npx tsx scripts/spec-delta.ts",
    "push": "npx tsx scripts/push.ts",
    "validate-readme": "npx tsx scripts/validate-readme.ts",
    "detect-dupes": "npx tsx scripts/detect-near-dupes.ts",
    "check": "npm run lint && npm run validate && npm run audit-assumptions && npm run scan-ambiguity && npm run check-cross-refs && npm run validate-readme && npm run detect-dupes",
    "check-cross-refs": "npx tsx scripts/check-cross-refs.ts",
    "assemble": "npx tsx scripts/assemble.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "markdownlint-cli": "^0.49.1",
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": ">=6"
  }
}
